Sample code in this directory:

trivial.pl	The most trivial complete program: fetch and print a record
fetch1.pl	The same as trivial.pl but with rudimentary error checking
simple.pl	Similar, but takes command-line args are prints all records
multiplex.pl	Searches concurrently across multiple servers
